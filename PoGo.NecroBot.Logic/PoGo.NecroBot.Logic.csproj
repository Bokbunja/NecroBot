<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <LangVersion>latest</LangVersion>
    <Nullable>disable</Nullable>
    <ImplicitUsings>disable</ImplicitUsings>
    <AssemblyName>PoGo.NecroBot.Logic</AssemblyName>
    <RootNamespace>PoGo.NecroBot.Logic</RootNamespace>
    <!-- This builds against the FAKE client in Stubs/, not the real game API. -->
    <DefineConstants>$(DefineConstants);STUB_CLIENT</DefineConstants>
  </PropertyGroup>

</Project>
